# External Assurance Evidence

`templates/` contains blocked, sanitized evidence templates. They are schema examples, not proof that any production-assurance gate has passed.

Real evidence must be created outside the repository using the compiled `external-assurance-evidence` command and stored in an approved encrypted evidence system.
