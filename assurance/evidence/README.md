# Evidence Quarantine

Do not place real assurance evidence in this repository.

Use an encrypted directory outside the repository and run:

`node dist/scripts/external-assurance-evidence.js init /secure/path/phoenix-evidence`

Only sanitized hashes, non-personal roles, controlled references, and redacted artifact metadata are permitted.
