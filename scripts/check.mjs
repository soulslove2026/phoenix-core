import fs from"node:fs";const req=["package.json","README.md","SECURITY.md","VERSION.json","Dockerfile","src/config.mjs","src/logger.mjs","src/server.mjs","test/config.test.mjs","test/server.test.mjs"];const miss=req.filter(x=>!fs.existsSync(x));if(miss.length){console.error(`Missing: ${miss.join(", ")}`);process.exit(1)}const p=JSON.parse(fs.readFileSync("package.json"));const v=JSON.parse(fs.readFileSync("VERSION.json"));if(p.version!==v.version){console.error("version mismatch");process.exit(1)}for(const f of["node_modules",".env"])if(fs.existsSync(f)){console.error(`Forbidden: ${f}`);process.exit(1)}console.log("Phoenix repository checks passed.");
