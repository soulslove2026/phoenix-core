import test from "node:test";import assert from "node:assert/strict";import {loadConfig} from "../src/config.js";
const secret=Buffer.alloc(32,1).toString("base64url");
test("database identity config requires strong secrets",()=>{assert.throws(()=>loadConfig({PHOENIX_ENV:"test",PHOENIX_DATABASE_REQUIRED:"true",PHOENIX_DATABASE_URL:"postgres://x"}));const c=loadConfig({PHOENIX_ENV:"test",PHOENIX_DATABASE_REQUIRED:"true",PHOENIX_DATABASE_URL:"postgres://x",PHOENIX_IDENTITY_TOKEN_PEPPER:secret,PHOENIX_IDENTITY_NOTIFICATION_KEY:secret,PHOENIX_IDENTITY_PRIVACY_KEY:secret});assert.equal(c.version,"3.4.0");assert.equal(c.identitySessionIdleTtlSeconds,43200);});
test("idle TTL cannot exceed absolute TTL",()=>assert.throws(()=>loadConfig({PHOENIX_ENV:"test",PHOENIX_IDENTITY_SESSION_IDLE_TTL_SECONDS:"1000",PHOENIX_IDENTITY_SESSION_ABSOLUTE_TTL_SECONDS:"900"})));
