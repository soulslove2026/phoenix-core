import test from"node:test";import assert from"node:assert/strict";import{createPhoenixServer}from"../src/server.mjs";const logger={debug(){},info(){},warn(){},error(){}};async function run(fn){const app=createPhoenixServer({service:"phoenix-core",version:"3.0.0",environment:"test",host:"127.0.0.1",port:0,logLevel:"error",requestLimitBytes:1024},{logger});const a=await app.start();try{await fn(`http://127.0.0.1:${a.port}`)}finally{await app.stop("test")}}test("health",()=>run(async b=>{const r=await fetch(`${b}/health`);assert.equal(r.status,200);assert.equal((await r.json()).status,"healthy")}));test("ready",()=>run(async b=>{const r=await fetch(`${b}/ready`);assert.equal(r.status,200)}));test("404",()=>run(async b=>{const r=await fetch(`${b}/missing`);assert.equal(r.status,404)}));
